from pathlib import Path

# Get the current working directory
current_dir = Path.cwd()

# Print the current working directory (as a Path object)
print(current_dir)

# Get the string representation of the path
print(str(current_dir))
